# 17. Статус веток

> Снимок на **2026-06-23**. Данные получены из `git for-each-ref` и
> `git rev-list --left-right --count origin/main...origin/<branch>`.
> «впереди/позади» — относительно `origin/main` (`main` = эталон).

## Сводка

| Ветка | Дата | HEAD | Впереди main | Позади main | Состояние |
|-------|------|------|:---:|:---:|-----------|
| **`main`** | 06-23 | `7c2538e` | — | — | 🟢 эталон, релизная линия |
| **`claude/review-and-fix-action-thycg1`** | 06-23 | `9ec8871` | **7** | 0 | 🟢 **актуальная рабочая** (PR #4) |
| `claude/code-review-plk01k` | 06-23 | `e71898c` | 0 | 5 | ⚪ влита в main, обсолетна |
| `claude/project-code-review-c6vcdj` | 06-22 | `ea7a66e` | 0 | 7 | ⚪ влита в main, обсолетна |
| `claude/gentleman-os-plan-cgm6hh` | 06-22 | `ff32c59` | 22 | 30 | 🟠 расходящаяся, стек V2.5–V2.11 |
| `claude/documentation-review-rdnwur` | 06-22 | `0cc507c` | 21 | 30 | 🟠 расходящаяся (подмножество plan) |
| `claude/garment-wear-forecast-card-5o5oe8` | 06-22 | `f059fe0` | 20 | 30 | 🟠 расходящаяся (подмножество plan) |

Легенда: 🟢 живая/эталон · ⚪ полностью в main, можно удалять · 🟠 сильно
разошлась, простой merge не пройдёт.

---

## Какая актуальная

**`claude/review-and-fix-action-thycg1`** — текущая рабочая ветка и единственная
с незавершённой интеграцией (открыт **PR #4 → `main`**). Она содержит весь
`main` (0 коммитов позади) плюс 7 своих:

```
9ec8871 docs: актуализировать журнал отладки, обработку ошибок и CI
15377de feat(logging): выгрузка журнала отладки в .md через «Поделиться»
48e910e fix(logging): импортировать PlatformDispatcher из dart:ui
996db57 fix(logging): наполнить журнал отладки ошибками и навигацией
0d76bf2 feat(wardrobe): индикатор износа на карточке вещи в сетке
30fff48 feat(dashboard): поддерживающий «Совет дня», когда всё в форме
a106d82 fix(biohacking): корректная объяснимость в maxImpactActions
```

Содержательно: наполнение журнала отладки (глобальный перехват ошибок +
навигация), экспорт журнала в `.md`, индикатор износа на карточке вещи,
честный «Совет дня», объяснимость биохакинга и актуализация документации.
CI на ветке зелёный (кроме исторического падения на `996db57`, исправленного
в `48e910e`).

---

## main (эталон)

Релизная линия. Последние коммиты — завершение фазы **V2.4 «Прогноз износа»** и
закалка CI:

```
7c2538e feat(wardrobe): человекочитаемый статус износа в прогнозе
a5b7c7c docs(roadmap): отмечена выполненной фаза V2.4 (прогноз износа)
d3d9054 feat(wardrobe): учёт ткани в прогнозе износа
3e5b284 feat(wardrobe): прогноз износа вещи на экране детали (V2.4 UI)
5e0a12c fix(ci): актуальные версии экшенов + тихий Codecov + имена артефактов
```

---

## Обсолетные ветки (полностью в main)

Эти ветки не содержат ничего, чего нет в `main` (0 коммитов впереди) — их работа
уже влита. Можно удалять после подтверждения:

- **`claude/code-review-plk01k`** (`e71898c`) — ревью/фиксы; на её основе
  собиралась текущая рабочая ветка, теперь всё в `main`.
- **`claude/project-code-review-c6vcdj`** (`ea7a66e`) — ревью проекта, блок
  под-оценок и «Совет дня» (фаза V2.1).

---

## Расходящиеся ветки (внимание: простой merge не пройдёт)

Три ветки от **2026-06-22** разошлись с текущей линией `main`: каждая
одновременно **впереди на ~20** своих коммитов и **позади на 30** коммитов
`main`. Это значит, что `main` после их создания ушёл далеко в другую сторону
(линия ревью/фиксов + V2.4), а в самих ветках накоплен **отдельный стек фич
V2.5–V2.11**, который в `main` так и не попал.

- **`claude/gentleman-os-plan-cgm6hh`** (`ff32c59`) — самая полная: включает
  merge PR #2 (`documentation-review`) и весь нижележащий стек. Уникальное:
  удаление `custom_lint`/`riverpod_lint` (фикс конфликта `pub get`), миграция
  схемы v7, актуализация документации против схемы v6, фиксы XP/привычек.
- **`claude/documentation-review-rdnwur`** (`0cc507c`) — подмножество предыдущей:
  ревью документации, тесты фазы V2.11, фикс `XpType.habits`.
- **`claude/garment-wear-forecast-card-5o5oe8`** (`f059fe0`) — подмножество:
  фичи **V2.10** (мульти-фильтр гардероба, быстрое выполнение привычки, контекст
  стиль-советника, напоминания здоровья) и **V2.11** (расширение тестов).

> ⚠️ Эти ветки нельзя влить fast-forward. Любая интеграция их фич потребует
> `merge`/`rebase` на актуальный `main` с разбором конфликтов (затронуты схема
> БД, документация и pubspec). Решение по ним стоит принимать отдельно: какие
> фичи V2.5–V2.11 нужны — перенести их поверх свежего `main`, а сами ветки
> затем закрыть.

---

## Рекомендации

1. **Довести PR #4** (`review-and-fix-action-thycg1`) до зелёного CI и влить в
   `main` — это ближайший актуальный инкремент.
2. **Удалить обсолетные** `code-review-plk01k` и `project-code-review-c6vcdj`
   (их содержимое уже в `main`).
3. **Разобрать расходящиеся ветки** отдельной задачей: оценить, какие фичи
   V2.5–V2.11 актуальны, и перенести их на свежий `main`; устаревшие — закрыть,
   чтобы не плодить параллельные истории схемы БД.
